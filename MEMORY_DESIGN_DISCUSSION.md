# 记忆架构讨论记录（Design Discussion）

> 状态：**讨论稿 / 未定案**。本文件只记录想法与权衡，**不是结论、未改任何代码**。
> 配套文件：具体实现计划见 [`ALWAYS_ON_RECALL_PLAN.md`](./ALWAYS_ON_RECALL_PLAN.md)（针对"自动 recall hook"那一块）。
> 目的：把这次脑暴的思路、技术约束、待决策点存档，方便后续继续讨论。
> 记录日期：2026-05-30。

---

## 0. 术语对照（避免后续讨论歧义）

| 口语说法 | 准确术语 | 是什么 |
|---|---|---|
| 理解助手 / 大块头模型 | **embedder（嵌入器）** | 把一段文字变成"含义坐标"（向量）的模型，如默认的 `multilingual-e5-small` |
| 含义坐标 | **向量 / embedding** | 一串数字，相近含义→相近坐标；靠 cosine 距离比相似度 |
| 字典查表式向量 | **静态向量 / static embeddings** | 预先算好每个词/词块的坐标，查询时只查表+平均，不跑神经网络 |
| 钉住的便利贴 | **pin / standing directive** | 每个 turn 无条件注入的常驻规则（不走语义搜索） |
| 向量库 | **PistaDB（`.pst`）** | Engram 存向量、做 KNN 检索的地方 |

---

## 1. 出发点（这次讨论的总目标）

把 Engram 的常驻层从"只推 pin"升级为：**每个 turn 由 harness 自动用用户当前 prompt 做语义 recall，把"相关"记忆也推进上下文** —— 让相关记忆"永远在场"，不依赖模型自觉去跑 `recall.py`。

> 机制基础（已确认）：Claude Code 的 `UserPromptSubmit` hook 能拿到本轮 `prompt`，其 stdout 会被注入上下文。现有 `engram directives` 已在用这条管道（但只推 pin）。

---

## 2. 讨论主线一：延迟问题 & 解决路线

### 2.1 核心障碍
"找**相关**记忆"必须**真的搜索/理解 prompt**，这要用 embedder。而 hook **每条消息都是全新子进程**；默认本地 e5 **每次冷启动 torch + 模型约 2–4 秒** → 每 turn 都付这个成本，不可接受。

> 对比：pin/directives 之所以能秒注入，是因为它**根本不搜索**，只是把钉住的便利贴原样念一遍。

> 已有的 `engram-mcp`（常驻、e5 只加载一次）是被 client 经 stdio 拉起的，**hook 子进程够不到**；`engram warmup` 是按进程预热，对全新子进程也无用。

> **常见疑问：默认不是已经用本地 e5 了吗？** 是的，默认就是本地 `multilingual-e5-small`，不联网、不用 API。但**"本地" ≠ "启动快"**：e5 是 transformer，算的是整句"含义坐标"，每次用前要 `import torch` + 加载权重（≈2–4s）。hook 每条消息都是全新进程、跑完即卸载，于是**每 turn 都要从头"叫醒"一次**。所以缺的不是"本地模型"，而是"**让它在消息之间一直醒着**"——这正是路线 ②（`engram serve` 常驻保温**你已有的那个 e5**）要补的；③ 则是换成根本不用叫醒的查表式向量。

### 2.2 一条硬约束（很重要，别踩）
**存进 PistaDB 用的是哪套向量坐标，查询就必须用同一套。** 不能在 e5 坐标存的库上临时挂一个别的向量来查——两套对不上，结果是乱的。换嵌入方式 ⇒ **存和查一起换 + 已有记忆全部重算（维度变了要重建库）**。

### 2.3 一个澄清："用大模型分段"
- 在**存**的时候让主模型（我）把对话切成语义块再存：可行，我顺手就能做。
- 但在**查**的时候（hook 触发那刻）**现场没有免费的大模型**：hook 是独立小程序，要用大模型得联网调 API，每条消息一次，反而更慢更贵。
- 结论：**分段帮"存"，但解决不了"查得快"**；查得快靠的是"轻量向量"那一步。

### 2.4 四条路线（含 user 提出的 ③）

| | ① 借现成待命助手 | ② 自造常驻后台 | ③ 换查表式轻量向量 **(user 思路)** |
|---|---|---|---|
| 做法 | 用一直待命的 **Ollama（本地、免费、离线）** 或 **在线 API** 当 embedder | 写 `engram serve` 后台进程，e5 只加载一次、长期待命，hook 当瘦客户端去问 | 用 **model2vec（静态蒸馏向量）**：查表+平均，毫秒级、不用 torch |
| 你要额外装/配 | 要（Ollama 或 API key） | 不用 | 不用 |
| 我们的开发量 | 小，最快 | 大 | 中（加后端 + 重算库） |
| 长期维护 | 几乎没有 | 多一个后台进程要照看 | 几乎没有 |
| 召回质量 | 满血 e5 | 满血 e5 | 略降，**中文待实测** |
| 备注 | "C 方案"= 先①后② 分阶段 | 让默认 e5 也能 warm | 见 §2.5 |

### 2.5 关于 ③（model2vec / 静态蒸馏向量）
- 是 user "别用大块头助手、改查词向量表"思路的**现代高质量版**。
- 传统 word2vec/GloVe 太笨、中文还要额外分词；**model2vec 是把 e5 这类模型"蒸馏"成一张静态查表**：极快、内存小、不用 torch，语义比传统词向量强很多（只比满血 e5 略弱），有多语言版本。
- **唯一不敢打包票：中文召回质量** → 稳妥做法是先做个小实验（用 model2vec 把几条中文记忆重算、跑几个查询看准不准）再定。
- 与 §3 两层架构**互相加成**：精炼记忆短而干净，正是静态向量表现最好的场景；且换嵌入器只需重算少量精炼记忆。

### 2.6 实测数据（user 机器，Windows / Py3.13，2026-05-30）

全新 Python 进程 = 模拟每个 turn 的 hook 真实成本。**每次用不同 query 强制绕开 `.pcc` 嵌入缓存**（同一 query 第二次会命中缓存、~0.5s，但 hook 每轮都是新问题，缓存命中不代表真实成本）。

每轮 = 每次都用**不同的新问题**（强制绕开 `.pcc` 缓存，等于 hook 真实场景）：

| 路径 | 每轮（新问题） | 说明 |
|---|---|---|
| A) 当前 `directives`（不加载模型） | **~0.5 s** | 现廉价 hook；证实 embedder 是**懒加载** |
| B) e5 recall，**联网**（默认现状） | **~25–30 s** | 每次还白白联网查 HF（占 ~13–17s！） |
| B') e5 recall，**离线** | **~13 s**（极稳：12.7/12.8/12.8） | torch+ST import ~9s + 模型加载 ~1–3s |
| C) 无 torch（hash，≈model2vec 类） | **~0.35 s** | 纯 numpy，与廉价 hook 同量级 |

**拆解（离线）**：`import torch` ~2.3s + `import sentence_transformers` ~6.5s + 模型加载 ~1–3s + embed（热）~0.03s。大头是两个 import（~9s），每个新进程躲不掉。

**三条结论**：
1. **白捡的坑（该单独修）**：模型已本地缓存却仍联网查 HF，白费 **~13–17s/次**（联网 ~28s vs 离线 ~13s，差出一半多）。缓存命中时强制 `local_files_only`/离线，可让**任何** recall 立刻提速；与自动 recall 决策无关。**但救不了 hook**——离线仍 ~13s。
2. **e5 直塞 hook 彻底不可行**：即便离线仍 ~13s/轮，大头是 torch/ST import（~9s），只要每 turn 起新进程用 torch 系就躲不掉。
3. **③(查表式) 被数据强支持**：无 torch 冷启动 ~0.35s，**与现廉价 hook 同量级** → model2vec 有望"每轮 ~0.5–0.9s 的语义 recall"，**不用 Ollama/API/daemon**。

**用数据重排三条路**：

| 路线 | 每轮 | 质量 | 额外负担 |
|---|---|---|---|
| ③ model2vec | **~0.5–0.9s** | 略降，**中文待测** | 几乎没有 |
| ② 常驻 daemon+e5 | 首轮 ~13s，后续 ~0.5s | 满血 e5 | 多养后台进程 |
| ① Ollama/API | ~1s | 满血 | 装 Ollama / API key |
| ~~e5 直塞 hook~~ | ~~13s 离线 / ~28s 联网~~ | 满血 | 不可用 ❌ |

> ~0.5s 是 hook 进程启动地板（Python + import engram + 读库），三条可行路线都要付；区别只在要不要再叠 ~10s 的 e5 成本。
> **当前最大未知 = ③ 的中文召回质量**（需一次 model2vec 小实验验证）。
>
> 附（中间路线）：用 **ONNX/optimum** 跑 e5 可绕开 torch 那 ~9s import，或许能把满血质量压到 ~3–5s/轮（仍非亚秒，但比 13s 好），代价是多一层导出+依赖；列此备忘，非主选。

---

## 3. 讨论主线二：两层记忆架构（精炼 / 原始）—— user 的新想法

### 3.1 想法本体
把记忆分两种：
1. **精炼记忆**：从对话总结提取的高信号要点。**存进向量库（PistaDB）**，可被语义搜索。
2. **原始对话**：完整逐字记录。**存进普通数据库**（不进向量库、不直接被搜）。
3. **指针**：一条精炼记忆**指向多个原始对话**，且**由这多个原始对话提炼而来**；要看原文需通过**特定工具**按指针去取。

### 3.2 评价：方向非常对，是被验证过的成熟架构
等价于业界的"**索引-原文分离 / 两层记忆**"：
- LangChain **Parent-Document Retrieval**（嵌入小摘要、返回大原文）；
- 斯坦福 *Generative Agents* 的 **reflection**（从大量原始观察提炼"反思"，可回溯证据）；
- 认知科学 **情景记忆 vs 语义记忆**（原始经历 vs 提炼知识）。

### 3.3 比方
- **精炼记忆 = 索引卡**（一句话总结 + 背面写"出处第几页"）→ 进向量库，小而精、好搜。
- **原始对话 = 书架上的原文** → 进普通库，便宜、海量、不进目录。
- 查询：**先翻索引卡**（向量检索），要细节再**按页码去书架取原文**（特定工具）。

### 3.4 在 Engram 里怎么落地（是扩展，不是重写）

| 概念 | 对应现状 / 需新增 |
|---|---|
| 精炼记忆（进向量库） | ≈ 现有一条 memory（`description` 已嵌入）。**新增字段** `raw_refs=[原始id...]` |
| 原始对话（进普通库） | **新增**：`.claude/engram/` 旁放 `raw.db`（标准库 `sqlite3`，零依赖、按 id 取） |
| "特定工具"取原文 | **新增** `engram trace <精炼id>`（类比现有 `expand.py`，按 `raw_refs` 去 sqlite 取） |
| "依靠多个原始对话产生" | **新增** 精炼/反思步骤（见 §3.6 待决策） |
| 原始对话从哪来 | Claude Code hook 能拿到 `transcript_path`（完整记录）→ 可在会话结束 hook **自动归档**进 sqlite，不用手动喂 |

### 3.5 与主线一的加成 ✨
1. **向量库更小更干净**（只放精炼、不塞逐字闲聊）→ 召回更准、更快、更省 → 直接利好"自动 recall"目标。
2. **让 ③(model2vec) 更香**：换嵌入器只需重算少量精炼记忆；精炼记忆短而干净，静态向量正好擅长。
3. **可回溯 / 可重炼**：原文不丢，能审计"它为什么这么认为我"，也能将来用更好的模型重新精炼。

### 3.6 必须想清楚的现实问题（坑）
1. **谁来精炼、何时精炼？（架构重心，待决策）**
   - 在线：我在对话里顺手提炼并保存（便宜，但只看得到当前会话）；
   - 离线批处理：后台任务定期把攒下的原文重新提炼（更整体，但要 LLM 调用成本 + 触发机制）。
   - 注意：现有 `consolidate` 是**纯向量合并、不带 LLM**；这一步是**真正的新能力**（可叫 `engram reflect`）。
2. **指针与删除**：删精炼记忆时原文留还是连删？跨两库 id 一致性、并发写（现有跨进程锁要覆盖两个库）。
3. **召回天花板变了**：原文不进向量库 ⇒ **没有精炼记忆指向的原文就搜不到** → 召回质量取决于精炼这一步。（将来可加"原文全文兜底搜索"，属后话。）
4. **隐私 / 体积**：逐字存全部对话（虽在本地）是隐私 + 磁盘考量，需保留/清理策略。
5. **复杂度**：多了一个库、一套跨库链接、一条归档管道，一致性维护成本上升。

### 3.7 倾向性建议
值得做，但比"自动 recall hook"是**更大一块**，建议**分阶段**：先做"两层存储 + `trace` 工具 + 在线精炼"（小而完整），离线反思 `engram reflect` 作为后续。

---

## 4. 待决策清单（汇总，后续讨论用）

**关于延迟/嵌入方式（主线一）**
- [ ] D1：走 ① 借现成（Ollama/API）／ ② 自造常驻 daemon ／ ③ model2vec 静态向量？
- [ ] D2：若走 ③，先做中文召回质量小实验再定？（推荐）
- [ ] D3：默认 e5 用户在加速就绪前，relevant 段"超时降级为 directives-only"是否可接受？

**关于两层架构（主线二）**
- [ ] D4：**精炼记忆何时产生** —— 在线（我顺手）／ 离线（`engram reflect`）／ 两者结合（倾向）／ 暂只存原文手动精炼？
- [ ] D5：原始对话用 `sqlite3` 普通库存放、放 `.claude/engram/raw.db`？
- [ ] D6：删除精炼记忆时，原文级联删还是保留？
- [ ] D7：原始对话归档触发点（会话结束 hook 自动 / 我显式保存 / 两者）？
- [ ] D8：隐私与保留策略（存多久、能否关闭原文归档）？

**关于自动 recall hook（见 `ALWAYS_ON_RECALL_PLAN.md` §9）**
- [ ] 命令命名、是否完全只读不 bump、输出形态、注入类型范围、是否加 SessionStart 等。

---

## 5. 当前共识（已基本对齐的点）
- 目标方向：相关记忆"永远在场"，由 harness 推送。✅
- pin/directives 那条管道不受影响、永远秒注入。✅
- "同一向量空间"是硬约束；换嵌入方式要两边一起换 + 重算。✅
- 两层记忆（精炼/原始分离）是好架构、值得纳入设计。✅
- 这些都还在**讨论稿**阶段，**未动代码**；继续讨论后再定案、再写实现计划。✅

---

## 6. 下一步（建议）
1. 继续敲定 §4 的待决策（尤其 D1/D4 两个重心）。
2. 若倾向 ③，先做 model2vec 中文小实验。
3. 决策稳定后，把两层架构正式并入 `ALWAYS_ON_RECALL_PLAN.md`（或拆成单独的实现计划），再开始编码。

> 备注：本轮全程未修改源码；`tests/` 之前的 UTF-8 修复是上一个任务的内容，与本讨论无关。
